import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 8) {
            Text("Activity Weather")
            Text("Weather-based activity rankings")
        }
        .padding()
    }
}
